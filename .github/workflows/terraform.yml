name: Terraform Deploy

on:
  workflow_dispatch:

jobs:
  terraform:
    runs-on: ubuntu-latest

    env:
      TERRAFORM_DIR: ${{ github.workspace }}/terraform


    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.8.5

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-1

      - name: Terraform Init
        working-directory: ${{ env.TERRAFORM_DIR }}
        run: terraform init -upgrade

      - name: Terraform Plan
        working-directory: ${{ env.TERRAFORM_DIR }}
        run: terraform plan 

      - name: Terraform Apply
        working-directory: ${{ env.TERRAFORM_DIR }}
        # run: terraform state show aws_launch_template.app_lt
        run: terraform destroy -auto-approve
        env:
          TF_VAR_key_name: ${{ secrets.KEY_NAME }}
        # run: rm -rf .terraform 
        # run: aws ec2 run-instances --launch-template LaunchTemplateId=lt-0b20f6f46eef078c2,Version=$Default --dry-run


      - name: Success Message
        if: success()
        run: echo "✅ Terraform workflow completed successfully!"

      - name: Failure Message
        if: failure()
        run: echo "❌ Terraform workflow failed. Check logs above!"
